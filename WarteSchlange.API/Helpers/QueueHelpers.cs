using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using WarteSchlange.API.Models;

namespace WarteSchlange.API.Helpers
{
    public class QueueHelpers
    {
        public static string GenerateQueueIdentification(int queueId)
        {
            //var context = new MainContext();


            //var queueIdentifications = context.QueueEntries.Where(x => x.QueueId == queueId).Select(x => x.IdentificationCode).Distinct();
            string newIdentification = string.Empty;

            //do
            //{
            //    Random r = new Random();
            //    r.Next();
            //} while (queueIdentifications.Contains()

            return null;
        }
    }
}
